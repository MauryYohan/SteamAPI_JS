export default class Friend {
    constructor(steamid, relationship, friend_since) {
        this.steamid = steamid;
        this.relationship = relationship;
        this.friend_since = friend_since;
    }
}